function [] = plotRidge( tX, tY, k, lambda_range )
%PLOTRIDGE Summary of this function goes here
%   Detailed explanation goes here
    for lambda=lambda_range
        testE,trainE = kFold(tX,tY,k,
    end

end

